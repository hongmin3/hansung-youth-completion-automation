# 청년 양육 수료내역 디모데 자동입력 사양서

<!-- spec-template: v1 -->

| 항목 | 값 |
|---|---|
| Document Version | 1.0.0 |
| Project Version | 1.0.0 |
| Last Updated | 2026-09-19 |
| Status | active |
| Owner | 디모데 입력 운영 담당자 (역할명) |

이 문서는 `dimode-completion-input`가 **어떻게 동작해야 하는가**를 정의하는 기준이다.
코드가 현재 그렇게 동작한다는 사실은 사양이 아니다. 사양과 구현이 다르면 코드에 맞춰 이
문서를 고치지 말고 `SPEC / CODE MISMATCH`로 식별한다 — 절차는 `AGENTS.md`에 있다.

프로젝트 규모에 따라 아래 Section은 줄일 수 있다. 다만 **1 목적 · 2 범위 · 5 기능 요구사항 ·
9 오류 처리 · 11 테스트 사양 · 12 추적성**은 유지보수형 자동화라면 반드시 채운다.

## 1. 목적

청년부 교육 담당자가 Google Sheet에 모아 둔 양육 수료자 명단을, 사람이 디모데 웹 화면에서
한 명씩 검색·입력하는 대신 자동으로 등록한다. 수백 행 규모의 반복 입력에서 사람이 저지르는
동명이인 오입력과 중복 입력을 없애는 것이 목적이다.

성공 조건: 명단의 각 행이 **확정된 동일인에게만** 수료내역으로 등록되고, 등록에 성공했거나
이미 같은 과정이 등록되어 있던 행만 Sheet의 `입력여부`가 체크된다. 확정하지 못한 행은
입력하지도 체크하지도 않고 사유가 남는다.

## 2. 프로젝트 범위

### 포함

- Google Sheet(또는 동일한 열 구조의 CSV)에서 수료자 명단을 읽는 것.
- 디모데 `청년관리 > 교인검색(청년)` 화면에서 명단의 사람을 찾아 동일인으로 확정하는 것.
- 허용된 `청년-` 교육과정에 한해 수료내역을 등록하는 것.
- 같은 과정이 이미 등록되어 있는지 확인해 중복 등록을 막는 것.
- 성공한 행에 한해 Sheet의 `입력여부` 체크박스와 보완된 전화번호를 갱신하는 것.
- 행 단위 처리 결과를 CSV와 로그로 남기는 것.
- 실제 입력 없이 대상과 판정 결과만 확인하는 드라이런.

### 제외

- 명단 자체의 수집·정제. 시트 내용의 오탈자 교정은 사람이 한다.
- 디모데에 존재하지 않는 교인의 신규 등록. 교인 생성은 하지 않는다.
- `기초반`, `예배학교`, `결혼예비학교`의 입력. 현재 명시적 제외 대상이다.
- 수료 외 상태(미수료, 수강중 등)의 등록.
- 교육과정 삭제·수정. 이 자동화는 신규 등록만 한다.
- 스케줄 실행·무인 실행. 사람이 지켜보는 상태에서 수동 실행한다.
- Apps Script 영역. 그 기준은 `../newacts-newcomer-automation/`에 있다.

## 3. 시스템 구성

| 구성 요소 | 책임 |
|---|---|
| `main.py` | CLI 진입점. `completion_automation.main()`을 호출한다. |
| `completion_automation.py` | 옵션 파싱, 시트 읽기, 디모데 로그인·검색·동일인 판정·중복 확인·저장, 시트 갱신, 결과 기록 전체. |
| `config.example.py` | 공개 가능한 설정 항목의 예시. 복사해서 `config.py`로 쓴다. |
| `config.py` | 이 머신 전용 비밀 설정. 저장소에 포함하지 않는다. |
| `requirements.txt` | Python 의존성 목록. |
| `knowledge/` | 에이전트 작업용 판단 규칙. 사양을 복제하지 않고 참조만 한다. |
| `output/` | 실행 산출물(드라이런 계획 CSV, 실제 실행 결과 CSV) 저장 위치. |

외부 시스템 경계는 셋이다.

1. **Google Sheets** — 명단의 원본이자 `입력여부`의 기록처. Sheets API(OAuth) 또는
   CSV export로 읽고, 쓰기는 Sheets API로만 한다.
2. **디모데 웹(청년 관리)** — Playwright Chromium으로 조작하는 ASP.NET 프레임 기반 화면.
   공개 API가 없으므로 DOM selector에 의존한다.
3. **로컬 파일 시스템** — 설정, OAuth 자격 증명, 결과 CSV, 실행 로그.

## 4. 전체 동작 흐름

1. CLI 옵션을 해석해 실행 모드(드라이런/실제 입력)와 대상 범위를 정한다.
2. Google Sheets 자격 증명을 준비한다. 실제 입력 모드에서는 필수다(단, 체크 보류 옵션 제외).
3. 명단을 읽는다. 각 행에 원본 시트의 행 번호를 붙인다.
4. Playwright로 Chromium을 띄우고 디모데에 로그인해 교인검색(청년) 목록으로 이동한다.
5. 행마다:
   1. `입력여부`가 이미 체크되어 있으면 건너뛴다.
   2. `구분`을 허용 교육과정으로 매핑한다. 매핑이 없거나 제외 대상이면 `교육과정매핑없음`.
   3. 전화번호가 비어 있고 보완 조회가 켜져 있으면 이름·군·A/B로 전화번호를 보완한다.
   4. 전화번호가 있으면 전화번호로 검색해 `이름+전화번호`가 유일하게 맞는 사람을 찾는다.
   5. 찾지 못했으면 이름으로 검색해 `이름+군`이 같고 교인 구분이 청년 A/B인 사람이
      정확히 한 명일 때만 확정한다.
   6. 확정하지 못했거나 복수면 입력하지 않고 사유 상태를 남긴다.
   7. 드라이런이면 `드라이런확인`으로 끝낸다.
   8. 실제 입력이면 교인 상세 창을 열어 같은 과정이 있는지 보고, 없을 때만 저장한다.
   9. 저장 성공 또는 기존 등록 확인 뒤에만 Sheet의 `입력여부`를 체크한다.
6. 브라우저를 닫고 행 단위 결과를 `output/` 아래 CSV로 저장한다.

## 5. 기능 요구사항

ID 규칙: `REQ-<CATEGORY>-NNN`. CATEGORY는 대문자·숫자, NNN은 세 자리.
한 번 부여한 ID는 재사용하거나 의미를 바꾸지 않는다. 삭제한 ID를 다른 기능에 돌려쓰지 않는다.

기능 그룹 — CATEGORY마다 사람이 읽는 이름이다. `docs/SPEC.html`의 기능 목록이 이 이름으로 묶인다.

| 카테고리 | 이름 |
|---|---|
| SHEET | 시트 연동 |
| CLI | 실행 옵션 |
| FILTER | 대상 선별 |
| MATCH | 교인 매칭 |
| PHONE | 전화번호 보완 |
| INPUT | 수료내역 입력 |
| REPORT | 결과 보고 |
| SEC | 보안 |
| DATA | 개인정보 |
| OPS | 운영 |

### REQ-SHEET-001 수료자 명단 읽기

#### 목적
수료자 명단을 고정된 열 구조로 읽고, 각 행을 원본 시트의 행 번호와 묶어 둔다.

#### 동작
- 입력 원본은 세 가지 중 하나다: (a) Sheets API로 읽은 대상 탭의 A:G 범위,
  (b) `--sheet-csv`로 지정한 CSV 파일, (c) OAuth 자격 증명이 없을 때 쿠키로 내려받은
  CSV export.
- 열 순서는 `구분 / 군 / 팀 / 이름 / 핸드폰 / 담당자 / 입력여부`여야 한다.
- 첫 행은 header로 보고, 데이터 행에는 시트 기준 행 번호(2부터)를 부여한다. 이 번호가
  뒤의 시트 갱신과 결과 CSV의 `시트행` 값의 기준이다.
- API 응답에서 뒤쪽 빈 칸이 생략되어 열 수가 모자란 행은 빈 문자열로 채워 header와 길이를
  맞춘다.

#### 기대 결과
명단의 모든 데이터 행이, 원본 행 번호를 잃지 않은 채 처리 대상 목록으로 올라온다.

#### 예외 처리
- `SHEET_URL`에서 스프레드시트 ID를 뽑을 수 없으면 실행을 중단한다.
- 쿠키 방식에서 `401`이 오면 쿠키 만료임을 알리고 OAuth 설정을 안내하며 중단한다.
- `--sheet-csv`는 원본 시트의 전체 행 순서를 보존한 CSV에만 쓴다. 필터링된 CSV를 주면
  `시트행` 번호가 어긋나므로 사용자가 보장해야 한다.

#### 관련 구현
`completion_automation.py` — `load_sheet_rows`, `rows_from_csv`, `spreadsheet_id_from_url`,
`gid_from_url`

#### 관련 테스트
TEST-DRYRUN-001

### REQ-CLI-001 읽기 전용 기본 실행

#### 목적
기본값을 안전한 쪽(읽기 전용)에 두고, 실제 데이터 변경은 명시적 옵션으로만 일어나게 한다.

#### 동작
- 옵션 없이 실행하면 **드라이런**이다. 디모데와 Sheet를 변경하지 않는다.
- `--execute`를 줄 때만 디모데 저장과 Sheet 체크를 수행한다.
- `--limit N`은 실제로 처리한 행이 N개가 되면 멈춘다. 건너뛴 행(체크 완료 행)은 세지 않는다.
- `--skip-phone-lookup`은 전화번호 보완 조회를 생략한다(REQ-PHONE-001).
- `--sheet-csv PATH`는 Sheet 대신 CSV를 입력으로 쓴다.
- `--defer-sheet-checks`는 Sheets 자격 증명 없이 `--execute`를 허용하고, 체크 갱신을
  외부 작업으로 미룬다. 일반 실행에서는 쓰지 않는다.

#### 기대 결과
실행 시작 시 모드와 대상 행 수가 로그에 남는다. 옵션을 잘못 조합해도 드라이런이 실제
입력으로 바뀌지 않는다.

#### 예외 처리
- Playwright가 설치되어 있지 않으면 설치 명령을 안내하고 종료한다.
- `--execute`이면서 `--defer-sheet-checks`가 아닌데 OAuth 클라이언트 파일이 없으면
  입력을 시작하지 않고 중단한다.

#### 관련 구현
`main.py`, `completion_automation.py` — `parse_args`, `Options`, `main`

#### 관련 테스트
TEST-DRYRUN-001, TEST-EXEC-001

### REQ-FILTER-001 처리 대상 선별

#### 목적
이미 처리된 행과 입력해서는 안 되는 교육과정을 처리 대상에서 제외한다.

#### 동작
- `입력여부`가 체크로 해석되는 값이면 그 행은 검색조차 하지 않고 건너뛴다. 체크로 보는
  값은 `TRUE`, `Y`, `YES`, `완료`, `입력완료`, 체크 기호이며 대소문자를 구분하지 않는다.
- `구분` 값을 교육과정 매핑으로 변환한다. 기본 허용은 다음 다섯 개다.

  | 시트 `구분` | 디모데 과정 |
  |---|---|
  | LTC | 청년-LTC |
  | 확신반 | 청년-확신반 |
  | 성장반 | 청년-성장반 |
  | 성경대학 | 청년-성경대학 |
  | 교리대학 | 청년-교리대학 |

- 설정으로 매핑을 추가할 수 있으나, 제외 목록(`기초반`, `예배학교`, `결혼예비학교`)은
  설정으로 추가하더라도 코드가 다시 제거한다. 설정으로 제외를 뚫을 수 없어야 한다.
- 매핑 결과가 없거나 `청년-`으로 시작하지 않으면 `교육과정매핑없음`으로 남기고 입력하지 않는다.

#### 기대 결과
재실행해도 이미 체크된 행은 다시 처리되지 않는다. 제외 과정은 어떤 경로로도 디모데에
입력되지 않는다.

#### 예외 처리
허용 목록에 없는 `구분`은 오류가 아니라 정상적인 미처리 상태로 기록한다. 실행을 멈추지 않는다.

#### 관련 구현
`completion_automation.py` — `is_checked`, `course_map`, `process`

#### 관련 테스트
TEST-DRYRUN-001

### REQ-MATCH-001 전화번호로 교인 확정

#### 목적
전화번호가 있는 행은 전화번호를 1순위 식별자로 삼아 동일인을 확정한다.

#### 동작
- 시트의 `핸드폰`(또는 `전화번호`, `연락처`) 값을 숫자만 남겨 10자리/11자리일 때
  `000-0000-0000` 형태로 정규화한다. 그 외 길이는 번호 없음으로 본다.
- 정규화된 번호로 디모데를 검색하고, 결과 카드 중 `이름`과 `전화번호`가 모두 일치하는
  것을 고른다.
- 정확히 하나면 그 사람으로 확정한다. **군·팀이 달라도 동일인으로 판단한다** — 시트의
  소속이 낡았을 수 있고 이름+번호 조합이 더 강한 식별자이기 때문이다.
- 둘 이상이면 확정하지 않고 `교인복수일치`로 중단한다.

#### 기대 결과
번호가 있는 행은 소속 변경과 무관하게 동일인에 도달하거나, 안전하게 미처리로 남는다.

#### 예외 처리
번호로 확정하지 못하면(0건) 실패로 끝내지 않고 REQ-MATCH-002의 이름 기반 경로로 넘어간다.
복수 일치는 이름 경로로 넘어가지 않고 그 자리에서 중단한다.

#### 관련 구현
`completion_automation.py` — `format_phone`, `search_person_cards`, `parse_person_card`,
`find_exact_person_card`

#### 관련 테스트
TEST-DRYRUN-001

### REQ-MATCH-002 이름·소속으로 교인 확정

#### 목적
전화번호로 확정하지 못한 사람을 이름과 소속으로 확정하되, 조금이라도 모호하면 입력하지 않는다.

#### 동작
- 이름으로 검색하고, 결과 카드 중 다음을 **모두** 만족하는 것만 후보로 본다.
  - 이름이 시트의 `이름`과 같다.
  - `군`이 시트의 `군`과 같다. 비교 전에 괄호와 그 안의 세부 명칭을 제거해 정규화한다
    (예: `신군(아하)` → `신군`).
  - 디모데 교인 구분이 `교인 > 청년 (A)` 또는 `(B)`이다.
- 후보가 정확히 한 명일 때만 확정한다. 두 명 이상이면 `교인복수일치`, 0명이면
  `정확한교인없음`으로 남긴다.
- 이 경로로 확정한 사람에게 디모데 전화번호가 있으면 그 값을 시트 갱신 대상으로 삼는다
  (REQ-SHEET-002).

#### 기대 결과
동명이인이 있거나 청년 A/B가 아닌 사람은 절대 입력 대상이 되지 않는다.

#### 예외 처리
모호한 경우 사람이 판단하도록 미처리로 남긴다. 추측으로 한 명을 고르지 않는다.

#### 관련 구현
`completion_automation.py` — `normalize_group`, `same_name_and_group`,
`same_active_name_and_group`, `find_exact_person_card`

#### 관련 테스트
TEST-DRYRUN-001

### REQ-PHONE-001 빈 전화번호 보완

#### 목적
시트에 전화번호가 비어 있는 행에 대해, 디모데에서 번호를 찾아 채워 넣어 식별 정확도를 높인다.

#### 동작
- 번호가 비어 있고 `--skip-phone-lookup`이 꺼져 있을 때만 수행한다.
- 이름으로 검색해 `이름+군`이 같고 교인 구분이 청년 A/B이며 전화번호가 있는 후보를 모은다.
- 서로 다른 번호가 정확히 하나로 수렴할 때만 그 번호를 채택한다. 여러 개면 채택하지 않는다.
- 채택한 번호는 이후 REQ-MATCH-001의 검색에 쓰이고, 성공 시 시트에도 기록된다.

#### 기대 결과
번호 누락만으로 확정에 실패하는 행이 줄고, 시트의 번호 공란이 검증된 값으로 보완된다.

#### 예외 처리
보완에 실패해도 오류가 아니다. 번호 없는 상태로 REQ-MATCH-002 경로를 계속 진행한다.

#### 관련 구현
`completion_automation.py` — `enrich_phone`

#### 관련 테스트
TEST-DRYRUN-001

### REQ-INPUT-001 수료내역 중복 등록 방지

#### 목적
같은 사람에게 같은 교육과정이 두 번 등록되지 않게 한다.

#### 동작
- 저장 직전에 교인 상세 창에서 대상 교육과정명이 이미 목록에 있는지 확인한다.
- 있으면 저장하지 않고 상태를 `이미입력됨`으로 남긴다. 이는 실패가 아니라 정상 종료다.
- 중복 확인은 매 실행마다 디모데 화면에서 다시 수행한다. 시트의 체크 상태만 믿지 않는다.

#### 기대 결과
디모데 저장 뒤 시트 체크 전에 프로그램이 중단되어도, 재실행 시 중복 등록이 발생하지 않는다.

#### 예외 처리
상세 창을 열지 못하거나 확인할 수 없으면 저장하지 않는다. 확인 없는 저장은 허용하지 않는다.

#### 관련 구현
`completion_automation.py` — `process`

#### 관련 테스트
TEST-DUP-001

### REQ-INPUT-002 수료내역 등록

#### 목적
확정된 교인에게 정해진 값으로 수료내역을 등록한다.

#### 동작
- 교육과정: REQ-FILTER-001에서 매핑된 `청년-` 과정명을 정확히 일치로 선택한다.
- 상태: `수료`
- 수료일: `20260621`
- 수료연도: `2026`
- 강사명: 시트의 `담당자` 값
- 저장 후 설정된 대기 시간만큼 기다린 뒤 상태를 `입력완료`로 남긴다.
- 저장 여부와 무관하게 교인 상세 창은 닫는다.

#### 기대 결과
디모데 화면에서 사람이 직접 입력했을 때와 같은 값이 남는다.

#### 예외 처리
- 수료일·수료연도는 현재 코드 상수다. 기수가 바뀌면 사양과 코드를 함께 고쳐야 한다
  (13절 참조).
- 화면 요소를 찾지 못하면 그 행에서 실패한다. 선택자를 증거 없이 추측해 고치지 않는다.

#### 관련 구현
`completion_automation.py` — `process`, 상수 `COMPLETE_STATUS`, `COMPLETE_DATE`,
`COMPLETE_YEAR`

#### 관련 테스트
TEST-EXEC-001

### REQ-SHEET-002 입력여부 체크 동기화

#### 목적
Sheet의 `입력여부` 체크가 "디모데에 실제로 존재함"을 뜻하도록 유지한다.

#### 동작
- 체크는 **신규 저장 성공** 또는 **동일 과정 기존 등록 확인** 뒤에만 수행한다.
- 체크 대상 행은 REQ-SHEET-001이 부여한 원본 시트 행 번호로 지정한다.
- 같은 갱신에서, 보완된 전화번호가 있으면 `핸드폰` 열도 함께 기록한다.
- 확정 실패·복수 일치·과정 미매핑 행은 체크하지 않는다.

#### 기대 결과
체크되지 않은 행은 언제나 재실행 대상이 된다. 사람이 시트 값을 고친 뒤 다시 돌리면 된다.

#### 예외 처리
Sheets 자격 증명이 없으면 체크를 수행하지 않는다. 이 상태로 실제 입력을 하려면
`--defer-sheet-checks`를 명시해야 하며, 체크는 운영자가 별도로 맞춘다.

#### 관련 구현
`completion_automation.py` — `update_sheet_result`, `get_sheets_service`

#### 관련 테스트
TEST-EXEC-001

### REQ-REPORT-001 미입력 사유 보고

#### 목적
무엇을 왜 입력하지 않았는지 실행 후에 사람이 확인할 수 있게 한다.

#### 동작
- 처리한 행마다 다음 상태 중 하나를 남긴다.

  | 상태 | 의미 |
  |---|---|
  | `드라이런확인` | 교인을 확정했으나 저장하지 않음 |
  | `입력완료` | 신규 수료내역 저장 성공 |
  | `이미입력됨` | 동일 과정이 있어 저장하지 않음 |
  | `정확한교인없음` | 전화번호로도 이름·군·A/B로도 확정하지 못함 |
  | `교인복수일치` | 조건에 맞는 사람이 복수라 중단 |
  | `교육과정매핑없음` | 허용되지 않았거나 제외된 과정 |

- 결과는 `시트행 / 구분 / 군 / 팀 / 이름 / 핸드폰 / 담당자 / 교육과정 / 상태` 열의 CSV로
  `output/` 아래에 저장한다. 드라이런은 dry_run_plan.csv, 실제 입력은
  execution_result.csv다.
- 행을 처리할 때마다 한 줄씩 `result_log.txt`에 append 한다. 실행이 중간에 끊겨도 어디까지
  진행됐는지 남는다.

#### 기대 결과
실행 후 CSV만 보고 재작업 대상 행을 고를 수 있다.

#### 예외 처리
결과 CSV는 매 실행마다 같은 파일명으로 덮어쓴다. 보관이 필요하면 실행 후 사람이 복사한다.

#### 관련 구현
`completion_automation.py` — `save_plan`, `log`, `process`

#### 관련 테스트
TEST-DRYRUN-001

## 6. 비기능 요구사항

### NFR-SEC-001 인증 정보 저장소 밖 보관

#### 요구
인증 수단은 저장소 바깥에 두고, 어떤 산출물에도 값이 노출되지 않아야 한다.

- 디모데 계정, Google 쿠키, Sheets OAuth 클라이언트·토큰 JSON은 `config.py`와 로컬 JSON
  파일에만 존재한다. 이 파일들은 `.gitignore`에 등록되어 있어야 한다.
- `config.example.py`에는 자리표시자만 둔다. 실제 값이 들어가면 안 된다.
- 로그, 결과 CSV, 커밋 메시지, 에이전트 응답에 자격 증명을 복사하지 않는다.
- OAuth scope는 Sheets 접근 하나로 제한한다.

#### 측정
저장소 추적 파일에 자격 증명이 0건. `.gitignore`가 `config.py`, 인증 JSON, `output/`,
`user_data/`, `result_log.txt`, 디버그 산출물을 모두 포함한다.

### NFR-DATA-001 개인정보 로컬 취급

#### 요구
이 자동화가 다루는 값은 실명·연락처·소속을 포함하는 개인정보다. 취급 범위를 로컬로 제한한다.

- 결과 CSV, 실행 로그, 브라우저 프로필, 디버그 HTML·이미지는 로컬에만 남고 저장소에
  올라가지 않는다.
- 개인정보를 Knowledge, Evidence, 사양 문서, 이슈에 옮겨 적지 않는다. 필요하면 건수와
  사유 범주만 쓴다.
- 시트 쓰기는 `핸드폰`(E열)과 `입력여부`(G열) 두 칸으로 제한한다. 다른 열을 덮어쓰지 않는다.

#### 측정
저장소에 커밋된 개인정보 파일 0건. 시트 쓰기 대상 열 2개 고정.

### NFR-OPS-001 사람이 지켜보는 수동 실행

#### 요구
사람이 지켜보는 상태에서 수동으로 실행하며, 대상 사이트에 과부하를 주지 않는다.

- Chromium은 화면이 보이는 모드로 띄운다. 무인·headless 실행을 전제하지 않는다.
- 검색 후 기본 0.8초, 저장 후 기본 1.0초를 대기한다. 설정으로 늘릴 수 있다.
- 실행 순서는 항상 `--limit`을 건 드라이런 → 전체 드라이런 → `--execute --limit 1` →
  전체 실행이다.
- 스케줄러·CI에서 실행하지 않는다.

#### 측정
실행 1회당 검색 요청 간 최소 간격 0.8초. 무인 실행 경로 0개.

## 7. 데이터 사양

| 데이터 | 형태 | 위치 | 취급 |
|---|---|---|---|
| 수료자 명단 | 7열 표 (`구분 / 군 / 팀 / 이름 / 핸드폰 / 담당자 / 입력여부`) | Google Sheet 대상 탭 | 원본. 이 자동화는 E·G열만 쓴다. |
| 디모데 교인 정보 | 검색 결과 카드 텍스트에서 파싱한 이름·전화번호·군·팀·교인구분 | 메모리에만 존재 | 저장하지 않는다. |
| 처리 결과 | 9열 CSV | `output/` | 실행마다 덮어쓴다. 저장소 제외. |
| 실행 로그 | 행 단위 텍스트 append | `result_log.txt` (설정으로 변경 가능) | 누적. 저장소 제외. |
| 브라우저 프로필·디버그 산출물 | Playwright 데이터, HTML·PNG | 프로젝트 로컬 디렉터리 | 진단에 필요할 때만 열람. 저장소 제외. |
| 자격 증명 | 설정 모듈, OAuth 클라이언트·토큰 JSON | 프로젝트 로컬 | NFR-SEC-001. |

보존 기간은 정해져 있지 않다(13절 참조). 결과 CSV와 로그는 사람이 직접 정리한다.

## 8. Configuration 사양

설정은 `config.py` 하나에서 읽는다. 예시는 `config.example.py`에 있다.

| 항목 | 필수 | 기본값 | 의미 |
|---|---|---|---|
| `SHEET_URL` | 예 | 없음 | 대상 스프레드시트 URL. 여기서 ID와 gid를 추출한다. |
| `SHEET_TAB_NAME` | 아니오 | `통합 명단` | 읽고 쓸 탭 이름. |
| `GOOGLE_COOKIES` | 아니오 | 없음 | OAuth가 없을 때 CSV export용 쿠키. 만료되면 401. |
| `DIMODE_URL` | 예 | 없음 | 디모데 로그인 페이지 URL. |
| `USER_ID` / `USER_PW` | 예 | 없음 | 디모데 계정. |
| `GOOGLE_OAUTH_CLIENT_FILE` | 실제 입력 시 예 | 프로젝트 내 클라이언트 JSON | Desktop OAuth 클라이언트 파일 경로. |
| `GOOGLE_OAUTH_TOKEN_FILE` | 아니오 | 프로젝트 내 토큰 JSON | 승인 후 자동 생성·갱신되는 토큰 경로. |
| `EDU_COURSE_MAP` | 아니오 | 빈 dict | 기본 매핑에 더할 과정. 제외 목록은 추가해도 무시된다. |
| `LOG_FILE` | 아니오 | `result_log.txt` | 실행 로그 경로. 상대경로는 프로젝트 기준. |
| `SEARCH_DELAY` | 아니오 | 0.8 | 검색 후 대기 초. |
| `SAVE_DELAY` | 아니오 | 1.0 | 저장 후 대기 초. |

나머지는 CLI 인자로 받는다(REQ-CLI-001). 환경변수는 사용하지 않는다.

## 9. 오류 처리 정책

- **재시도하지 않는다.** 검색·저장 실패를 자동으로 다시 시도하지 않는다. 재시도는 사람이
  원인을 확인한 뒤 프로그램을 다시 실행하는 방식으로만 한다. 중복 방지(REQ-INPUT-001)가
  재실행을 안전하게 만든다.
- **부분 성공을 허용한다.** 확정 실패, 복수 일치, 과정 미매핑은 그 행의 상태로 기록하고
  다음 행으로 넘어간다. 한 사람 때문에 전체 실행을 멈추지 않는다.
- **중단 기준.** 다음은 행 단위 상태가 아니라 실행 중단 사유다.
  - `SHEET_URL`에서 스프레드시트 ID를 얻지 못함.
  - 쿠키 만료(401)로 명단을 읽지 못함.
  - `--execute`인데 필요한 OAuth 클라이언트 파일이 없음.
  - Playwright 미설치.
  - 로그인 실패나 화면 구조 변경으로 검색 화면에 도달하지 못함.
- **기록 위치.** 행 단위 결과는 `output/`의 CSV와 `result_log.txt`에 남는다. 로그는 행마다
  즉시 append 하므로 중단되어도 진행 지점을 알 수 있다. 단, 결과 CSV는 실행이 끝까지
  진행돼야 저장된다.
- **선택자 실패.** 디모데 화면 구조 변경이 의심되면 실제 입력을 멈추고 드라이런부터 다시
  확인한다. 증거 없이 선택자를 바꾸지 않는다.

## 10. 보안 요구사항

- 인증 수단은 둘이다: 디모데 ID/비밀번호(설정 파일), Google Sheets OAuth 토큰(로컬 JSON).
  쿠키 방식은 드라이런의 임시 수단으로만 허용하고 운영에는 쓰지 않는다.
- 저장소에 들어가면 안 되는 것: `config.py`, OAuth 클라이언트·토큰 JSON, 쿠키 값,
  `output/`, `user_data/`, `result_log.txt`, 디버그 HTML·이미지.
- 접근 범위: Google 쪽은 Sheets scope 하나, 디모데 쪽은 해당 계정이 원래 가진 청년관리
  권한을 넘지 않는다. 이 자동화는 권한을 확대하지 않는다.
- 토큰은 만료 시 refresh token으로 갱신하고, 갱신 결과를 같은 로컬 파일에 다시 쓴다.
- 에이전트는 설정 파일과 자격 증명 파일의 내용을 열어 응답에 싣지 않는다.

## 11. 테스트 사양

ID 규칙: `TEST-<CATEGORY>-NNN`.

**이 프로젝트에는 자동화된 테스트가 없다.** 테스트 프레임워크도, 테스트 파일도 없다.
아래 항목은 모두 사람이 수행하는 수동 검증 절차이며, 추적성 표의 Test 열에 `(수동)`으로
표시한다. 자동 테스트 도입 여부는 13절의 미확정 사항이다.

### TEST-SYNTAX-001

#### 검증 대상
REQ-CLI-001, REQ-REPORT-001 (구문 수준 회귀 방지)

#### 선행 조건
프로젝트 가상환경이 설치되어 있다. 외부 접근은 필요 없다.

#### 절차
1. 프로젝트 루트에서 `.venv/bin/python -m py_compile main.py completion_automation.py config.example.py`를 실행한다.

#### Expected Result
출력 없이 종료 코드 0. 이 검사는 문법만 본다 — 동작을 검증하지 않는다.

### TEST-DRYRUN-001

#### 검증 대상
REQ-SHEET-001, REQ-CLI-001, REQ-FILTER-001, REQ-MATCH-001, REQ-MATCH-002,
REQ-PHONE-001, REQ-REPORT-001

#### 선행 조건
설정이 완료되어 있고, 디모데 로그인과 시트 접근이 가능하다. 외부 조회가 발생하므로
사용자가 명시적으로 요청했을 때만 수행한다.

#### 절차
1. `python main.py --limit 5`를 실행한다.
2. 브라우저가 뜨고 로그인·검색이 진행되는 것을 화면으로 확인한다.
3. `output/` 아래 드라이런 계획 CSV를 연다.
4. 각 행의 `상태`가 `드라이런확인` / `정확한교인없음` / `교인복수일치` /
   `교육과정매핑없음` 중 하나인지 본다.
5. 번호가 비어 있던 행에 보완된 번호가 채워졌는지 본다.
6. 대상 시트를 열어 아무 값도 바뀌지 않았음을 확인한다.

#### Expected Result
디모데와 시트가 전혀 변경되지 않는다. 처리한 행 수가 5를 넘지 않는다. 각 행에 판정
사유가 남는다.

### TEST-EXEC-001

#### 검증 대상
REQ-INPUT-002, REQ-SHEET-002, REQ-CLI-001

#### 선행 조건
TEST-DRYRUN-001이 끝났고 결과에 이상이 없다. OAuth 승인이 완료되어 있다.
사용자가 실제 입력을 명시적으로 요청했다.

#### 절차
1. `python main.py --execute --limit 1`을 실행한다.
2. 디모데에서 그 교인의 상세 화면을 직접 열어 교육과정·상태·수료일·수료연도·강사명을
   눈으로 확인한다.
3. 시트에서 해당 행의 `입력여부`가 체크되었는지, 번호가 보완되었다면 `핸드폰`이
   채워졌는지 확인한다.
4. 결과 CSV에서 그 행의 상태가 `입력완료`인지 본다.

#### Expected Result
한 명만 입력된다. 디모데 값이 사양의 상수와 일치한다. 성공한 그 행만 체크된다.

### TEST-DUP-001

#### 검증 대상
REQ-INPUT-001

#### 선행 조건
TEST-EXEC-001로 입력된 사람이 있고, 그 행의 `입력여부` 체크를 수동으로 해제해 둔다.

#### 절차
1. `python main.py --execute --limit 1`을 그 행이 대상이 되도록 다시 실행한다.
2. 디모데 상세 화면에서 같은 교육과정이 하나뿐인지 확인한다.
3. 결과 CSV의 상태를 본다.

#### Expected Result
상태가 `이미입력됨`이고 교육과정이 중복 등록되지 않는다. 시트는 다시 체크된다.

### TEST-HYGIENE-001

#### 검증 대상
NFR-SEC-001, NFR-DATA-001

#### 선행 조건
없음. 자격 증명도 network도 쓰지 않는다 — git 추적 목록과 소스만 읽으므로 언제든 돌릴 수
있다. 이 프로젝트의 다른 TEST는 전부 수동 절차지만 이 둘의 '측정'은 기계가 답할 수 있다.

#### 절차
`.venv/bin/python -m unittest discover -s tests`를 실행한다
(`tests/test_repository_hygiene.py`).

#### Expected Result
`.gitignore`가 `config.py`, `credentials.json`, `token.json`, `output/`, `user_data/`,
`result_log.txt`, `debug_*.html`, `debug_*.png`를 모두 덮고, git이 그 경로들을 **실제로**
무시한다(선언과 동작을 따로 확인한다 — 뒤의 negation 규칙이 앞을 덮을 수 있다).
추적 중인 파일에 민감 산출물이 0건이고, `config.example.py`에 실제 스프레드시트 ID나
이메일 주소가 없다. `update_sheet_result`가 만드는 시트 range의 열이 정확히 `E`와 `G`이며,
시트에 쓰는 함수가 그 하나뿐이다(다른 쓰기 경로가 생기면 열 검사가 범위를 놓친다).

## 12. 요구사항 추적성

| Requirement | Implementation | Test | Status |
|---|---|---|---|
| REQ-SHEET-001 | `completion_automation.py` (`load_sheet_rows`) | TEST-DRYRUN-001 (수동) | verified |
| REQ-CLI-001 | `main.py`, `completion_automation.py` (`parse_args`, `main`) | TEST-DRYRUN-001 (수동) | verified |
| REQ-FILTER-001 | `completion_automation.py` (`is_checked`, `course_map`, `process`) | TEST-DRYRUN-001 (수동) | verified |
| REQ-MATCH-001 | `completion_automation.py` (`find_exact_person_card`) | TEST-DRYRUN-001 (수동) | verified |
| REQ-MATCH-002 | `completion_automation.py` (`same_active_name_and_group`) | TEST-DRYRUN-001 (수동) | verified |
| REQ-PHONE-001 | `completion_automation.py` (`enrich_phone`) | TEST-DRYRUN-001 (수동) | verified |
| REQ-INPUT-001 | `completion_automation.py` (`process`) | TEST-DUP-001 (수동) | implemented |
| REQ-INPUT-002 | `completion_automation.py` (`process`) | TEST-EXEC-001 (수동) | verified |
| REQ-SHEET-002 | `completion_automation.py` (`update_sheet_result`) | TEST-EXEC-001 (수동) | verified |
| REQ-REPORT-001 | `completion_automation.py` (`save_plan`, `log`) | TEST-DRYRUN-001 (수동) | verified |
| NFR-SEC-001 | `.gitignore`, `config.example.py` | TEST-HYGIENE-001 | verified |
| NFR-DATA-001 | `.gitignore`, `completion_automation.py` (`update_sheet_result`) | TEST-HYGIENE-001 | verified |
| NFR-OPS-001 | `completion_automation.py` (`main`, `search_person_cards`) | TEST-DRYRUN-001 (수동) | implemented |

Status 값: `draft` (사양만 있음) / `implemented` / `verified` (실제 실행까지 확인) /
`deprecated`.

`verified`의 근거는 `README.md`에 기록된 2026-08-11 실제 실행이다(대상 206행, 체크 174행,
요청에 따른 제외 31행, 확정 실패 1행). REQ-INPUT-001은 그 실행에서 `이미입력됨` 경로가
실제로 발생했는지 기록이 남아 있지 않아 `implemented`로 둔다.

## 13. 미확정 사항

- **수료일·수료연도가 코드 상수다.** `20260621` / `2026`이 고정되어 있어 다음 기수에는
  반드시 코드를 고쳐야 한다. 설정 항목이나 CLI 인자로 빼야 하는지, 그렇다면 시트의 열로
  받아야 하는지 정해지지 않았다.
- **제외 교육과정의 최종 처리.** `기초반`, `예배학교`, `결혼예비학교`는 "추후 확정 후
  사용자 요청 시"로만 적혀 있다. 디모데의 정확한 과정 명칭도, 입력할지 여부도 미확정이다.
- **호출되지 않는 팀 비교 함수.** 확정 경로는 `이름 + 군 + 청년 A/B`이고 `팀`은 판정에 쓰지
  않는다(2026-09-19 확정, 문서를 코드에 맞춤). `identity_matches`, `normalize_team`은
  정의만 되어 있고 호출되지 않는다 — 지울지, 시트 소속 확인용으로 쓸지는 정해지지 않았다.
- **`--defer-sheet-checks`의 후속 절차.** 미룬 체크를 누가 어떤 방법으로 맞추는지 정해진
  절차가 없다. 또한 이 옵션을 켜도 OAuth 파일이 존재하면 체크가 그대로 수행되는데, 이것이
  의도인지(자격 증명이 있으면 체크한다) 아니면 항상 보류해야 하는지 확정되지 않았다.
- **자동 테스트 도입 범위.** 현재 테스트가 없다. 순수 함수(`format_phone`,
  `normalize_group`, `is_checked`, `course_map`)만 단위 테스트로 덮을지, Playwright 경로도
  대상으로 할지 결정되지 않았다.
- **결과 CSV와 실행 로그의 보존 기간.** 개인정보를 담는데 정리 주기나 삭제 기준이 없다.
- **Owner.** 저장소에 운영 책임자가 명시되어 있지 않다.

## 14. 향후 개선 후보

- 수료일·수료연도·상태를 시트 열이나 CLI 인자로 받아 기수마다 코드를 고치지 않게 한다.
- 결과 CSV를 실행 시각이 들어간 파일명으로 저장해 이전 실행 결과를 덮어쓰지 않게 한다.
- 순수 함수에 대한 단위 테스트와, 저장된 HTML fixture로 `parse_person_card`를 검증하는
  테스트를 추가한다.
- 디모데 화면 selector를 한곳에 모아 UI 변경 시 수정 범위를 좁힌다.
- `정확한교인없음` 행만 모아 사람이 판단할 수 있게 후보 목록을 함께 출력한다.
