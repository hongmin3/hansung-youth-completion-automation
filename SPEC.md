# 청년 양육 수료내역 디모데 자동입력 사양서

<!-- spec-template: v1 -->

| 항목 | 값 |
|---|---|
| Document Version | 1.1.0 |
| Project Version | 1.0.0 |
| Last Updated | 2026-09-25 |
| Status | active |
| Owner | 디모데 입력 운영 담당자 (역할명) |

이 문서는 `dimode-completion-input`가 **어떻게 동작해야 하는가**를 정한 기준이다.
코드가 지금 그렇게 동작한다고 해서 그것이 사양이 되지는 않는다.

사양과 코드가 다르면 코드에 맞춰 이 문서를 고치지 않는다. `SPEC / CODE MISMATCH`로 표시해 알린다.
절차는 `AGENTS.md`에 있다.

프로젝트 크기에 따라 아래 절은 줄일 수 있다. 다만 유지보수하는 자동화라면 다음 절은 반드시 채운다.

- 1절 목적
- 2절 범위
- 5절 기능 요구사항
- 9절 오류 처리
- 11절 테스트 사양
- 12절 추적성

## 1. 목적

청년부 교육 담당자는 양육 수료자 명단을 Google Sheet에 모아 둔다. 지금까지는 사람이 디모데 웹
화면에서 한 명씩 검색해 입력했다. 이 자동화는 그 일을 대신해 수료내역을 자동으로 등록한다.

명단은 수백 행에 이른다. 같은 입력을 이만큼 되풀이하면 사람은 동명이인에게 잘못 입력하거나 같은
내용을 두 번 입력하기 쉽다. 이런 실수를 없애는 것이 목적이다.

성공 조건은 다음과 같다.

- 명단의 각 행은 **확정된 동일인에게만** 수료내역으로 등록된다.
- 시트의 `입력여부`는 등록에 성공한 행, 또는 같은 과정이 이미 등록되어 있던 행만 체크된다.
- 누구인지 확정하지 못한 행은 입력하지도 체크하지도 않고, 그 사유를 남긴다.

### 이 문서에서 쓰는 말

아래 말에는 `docs/SPEC.html`에서 마우스를 올리면 뜻이 보인다.

| 용어 | 뜻 |
|---|---|
| 디모데 | 교인 정보를 다루는 웹 화면이다. 이 자동화가 수료내역을 입력하는 곳이다. |
| 수료자 명단 | 교육을 마친 사람을 모아 둔 Google Sheet 표다. 열은 `구분 / 군 / 팀 / 이름 / 핸드폰 / 담당자 / 입력여부` 순서다. |
| 수료내역 | 디모데에 교인마다 남기는 교육과정 수료 기록이다. |
| 드라이런 | 디모데와 시트를 바꾸지 않고, 누구를 찾았고 어떻게 판정했는지만 확인하는 실행이다. 옵션 없이 실행하면 드라이런이다. |
| 실제 입력(`--execute`) | 디모데에 저장하고 시트에 체크까지 하는 실행이다. |
| 동일인 확정 | 명단의 한 행이 디모데의 어느 교인인지 한 사람으로 정하는 일이다. |
| 교육과정 구분(`구분`) | 시트에서 과정 이름이 적힌 열이다. 예: LTC, 확신반. |
| 소속 군(`군`) | 시트의 소속 열이다. 비교할 때는 괄호 부분을 뗀다(`신군(아하)` → `신군`). |
| `입력여부` | 시트 G열의 체크박스다. 체크되어 있으면 디모데에 등록되어 있다는 뜻이다. |
| `핸드폰` | 시트 E열의 전화번호 칸이다. 비어 있으면 디모데에서 찾아 채울 수 있다. |
| `담당자` | 시트의 담당자 열이다. 디모데의 강사명으로 들어간다. |
| 청년 A/B | 디모데의 교인 구분이 `교인 > 청년 (A)` 또는 `(B)`인 사람이다. |
| 교육과정 매핑 | 시트 `구분` 값을 `청년-`으로 시작하는 디모데 과정명으로 바꾸는 표다. |
| 제외 과정 | `기초반`, `예배학교`, `결혼예비학교`다. 지금은 입력하지 않는다. |
| 교인 상세 창 | 디모데에서 교인 한 명의 정보를 여는 창이다. 여기서 같은 과정이 있는지 보고 저장한다. |
| `시트행` | 결과 CSV에 적는 원본 시트의 행 번호다. 데이터 행은 2부터 센다. |
| 처리 상태 | 행마다 남는 처리 결과다. `입력완료`, `이미입력됨` 같은 여섯 가지가 있다(REQ-REPORT-001). |
| 선택자 | 프로그램이 디모데 화면에서 버튼이나 입력 칸을 찾을 때 쓰는 표시다. |
| OAuth | Google 계정으로 시트 접근을 허락받는 방식이다. 승인하면 토큰 JSON 파일이 생긴다. |
| 쿠키 방식 | OAuth 없이 `GOOGLE_COOKIES`로 시트를 CSV로 내려받는 방식이다. 드라이런의 임시 수단으로만 쓴다. |
| Playwright | 브라우저(Chromium)를 프로그램으로 조작하는 도구다. |
| 실행 옵션 | 실행 명령 뒤에 붙이는 설정이다. 예: `--limit N`. |
| `config.py` | 이 컴퓨터에만 두는 비밀 설정 파일이다. 저장소에 넣지 않는다. |
| `output/` | 결과 CSV를 저장하는 폴더다. |
| `result_log.txt` | 행을 처리할 때마다 한 줄씩 쌓이는 실행 로그다. |

## 2. 프로젝트 범위

### 포함

- Google Sheet(또는 같은 열 구조의 CSV)에서 수료자 명단을 읽는다.
- 디모데 `청년관리 > 교인검색(청년)` 화면에서 명단의 사람을 찾아 동일인으로 확정한다.
- 허용된 `청년-` 교육과정만 수료내역으로 등록한다.
- 같은 과정이 이미 등록되어 있는지 확인해 두 번 등록되지 않게 한다.
- 성공한 행에만 시트의 `입력여부` 체크박스와 새로 찾은 전화번호를 적는다.
- 행마다 처리 결과를 CSV와 로그로 남긴다.
- 실제로 입력하지 않고 대상과 판정 결과만 확인하는 드라이런을 제공한다.

### 제외

- 명단 자체를 모으고 다듬는 일. 시트 내용의 오탈자는 사람이 고친다.
- 디모데에 없는 교인을 새로 등록하는 일. 교인은 만들지 않는다.
- `기초반`, `예배학교`, `결혼예비학교` 입력. 지금은 입력하지 않기로 정해 둔 과정이다.
- 수료가 아닌 다른 상태(미수료, 수강중 등)의 등록.
- 교육과정 삭제·수정. 이 자동화는 새로 등록만 한다.
- 예약 실행, 사람 없이 돌리는 실행. 사람이 지켜보면서 직접 실행한다.
- Apps Script 쪽 일. 그 기준은 `../newacts-newcomer-automation/`에 있다.

## 3. 시스템 구성

| 구성 요소 | 책임 |
|---|---|
| `main.py` | 실행을 시작하는 파일이다. `completion_automation.main()`을 부른다. |
| `completion_automation.py` | 나머지 일을 모두 맡는다. 옵션 읽기, 시트 읽기, 디모데 로그인·검색·동일인 판정·중복 확인·저장, 시트 갱신, 결과 기록이다. |
| `config.example.py` | 공개해도 되는 설정 항목의 예시다. 복사해서 `config.py`로 쓴다. |
| `config.py` | 이 컴퓨터에만 두는 비밀 설정이다. 저장소에 넣지 않는다. |
| `requirements.txt` | 설치해야 하는 Python 패키지 목록이다. |
| `knowledge/` | AI 에이전트가 작업할 때 쓰는 판단 규칙이다. 사양을 베껴 두지 않고 참조만 한다. |
| `output/` | 실행 결과 파일(드라이런 계획 CSV, 실제 실행 결과 CSV)을 저장하는 곳이다. |

이 자동화가 바깥 시스템과 만나는 곳은 세 군데다.

1. **Google Sheets**: 명단의 원본이고, `입력여부`를 적는 곳이다. 읽을 때는 Sheets API(OAuth)나
   CSV 내려받기(export)를 쓴다. 쓸 때는 Sheets API만 쓴다.
2. **디모데 웹(청년 관리)**: Playwright Chromium으로 조작하는 화면이다. ASP.NET 프레임으로 되어 있다.
   공개 API가 없어서 화면 요소를 찾는 선택자(DOM selector)에 기댄다.
3. **로컬 파일 시스템**: 설정, OAuth 자격 증명, 결과 CSV, 실행 로그를 둔다.

## 4. 전체 동작 흐름

1. 실행 옵션을 읽어 실행 방식(드라이런 또는 실제 입력)과 처리할 범위를 정한다.
2. Google Sheets 자격 증명을 준비한다. 실제 입력에서는 반드시 있어야 한다. 단, 체크를 미루는 옵션을
   쓰면 없어도 된다.
3. 명단을 읽는다. 각 행에 원본 시트의 행 번호를 붙인다.
4. Playwright로 Chromium을 띄우고 디모데에 로그인한 뒤 교인검색(청년) 목록으로 간다.
5. 행마다 아래 "행 하나 처리하기" 차례를 따른다.
6. 브라우저를 닫고 행마다의 결과를 `output/` 아래 CSV로 저장한다.

행 하나 처리하기:

1. `입력여부`가 이미 체크되어 있으면 건너뛴다.
2. `구분`을 허용된 교육과정으로 바꾼다. 바꿀 과정이 없거나 제외 과정이면 `교육과정매핑없음`으로 남긴다.
3. 전화번호가 비어 있고 보완 조회가 켜져 있으면 이름·군·A/B로 전화번호를 찾아 채운다.
4. 전화번호가 있으면 전화번호로 검색한다. `이름+전화번호`가 딱 한 사람과 맞으면 그 사람으로 정한다.
5. 찾지 못했으면 이름으로 검색한다. `이름+군`이 같고 교인 구분이 청년 A/B인 사람이 정확히 한 명일
   때만 확정한다.
6. 확정하지 못했거나 여러 명이 맞으면 입력하지 않고 사유 상태를 남긴다.
7. 드라이런이면 `드라이런확인`으로 끝낸다.
8. 실제 입력이면 교인 상세 창을 열어 같은 과정이 있는지 본다. 없을 때만 저장한다.
9. 저장에 성공했거나 이미 등록된 것을 확인한 뒤에만 시트의 `입력여부`를 체크한다.

## 5. 기능 요구사항

ID 규칙: `REQ-<CATEGORY>-NNN`. CATEGORY는 대문자와 숫자로 쓰고, NNN은 세 자리 숫자다.
한 번 붙인 ID는 다시 쓰거나 뜻을 바꾸지 않는다. 지운 ID를 다른 기능에 돌려쓰지 않는다.

아래 기능 그룹 표는 CATEGORY마다 사람이 읽는 이름을 붙인다. `docs/SPEC.html`의 기능 목록이 이
이름으로 묶인다.

| 카테고리 | 이름 |
|---|---|
| SHEET | 시트 연동 |
| CLI | 실행 옵션 |
| FILTER | 대상 선별 |
| MATCH | 교인 매칭 |
| PHONE | 전화번호 보완 |
| INPUT | 수료내역 입력 |
| REPORT | 결과 보고 |
| SEC | 보안 |
| DATA | 개인정보 |
| OPS | 운영 |

### REQ-SHEET-001 수료자 명단 읽기

#### 목적
수료자 명단을 정해진 열 순서로 읽는다. 각 행에는 원본 시트의 행 번호를 함께 붙여 둔다.

#### 동작
명단은 다음 세 곳 가운데 하나에서 읽는다.

- (a) Sheets API로 읽은 대상 탭의 A:G 범위
- (b) `--sheet-csv`로 지정한 CSV 파일
- (c) OAuth 자격 증명이 없을 때 쿠키로 내려받은 CSV(export)

읽은 명단은 이렇게 다룬다.

- 열 순서는 `구분 / 군 / 팀 / 이름 / 핸드폰 / 담당자 / 입력여부`여야 한다.
- 첫 행은 머리글로 본다. 데이터 행에는 시트 기준 행 번호를 2부터 붙인다.
- 이 번호는 뒤에서 시트를 고칠 때와 결과 CSV의 `시트행` 값을 적을 때 기준이 된다.
- API 응답은 행 끝쪽의 빈칸을 빼고 올 수 있다. 그래서 열 수가 모자란 행은 빈 문자열로 채워
  머리글과 길이를 맞춘다.

#### 기대 결과
명단의 모든 데이터 행이 처리할 목록에 올라온다. 각 행은 원본 행 번호를 그대로 지닌다.

#### 예외 처리
- `SHEET_URL`에서 스프레드시트 ID를 뽑을 수 없으면 실행을 멈춘다.
- 쿠키 방식에서 `401`이 오면 쿠키가 만료됐다고 알리고, OAuth 설정 방법을 안내한 뒤 멈춘다.
- `--sheet-csv`에는 원본 시트의 모든 행을 순서 그대로 담은 CSV만 쓴다. 일부 행만 걸러 낸 CSV를
  주면 `시트행` 번호가 어긋난다. 그래서 CSV가 모든 행을 담았는지는 사용자가 확인해야 한다.

#### 관련 구현
`completion_automation.py`: `load_sheet_rows`, `rows_from_csv`, `spreadsheet_id_from_url`,
`gid_from_url`

#### 관련 테스트
TEST-DRYRUN-001

### REQ-CLI-001 읽기만 하는 기본 실행

#### 목적
기본값은 안전한 쪽, 곧 읽기만 하는 쪽에 둔다. 실제로 데이터를 바꾸는 일은 옵션을 직접 줄 때만
일어난다.

#### 동작
- 옵션 없이 실행하면 **드라이런**이다. 디모데와 시트를 바꾸지 않는다.
- `--execute`를 줄 때만 디모데에 저장하고 시트에 체크한다.
- `--limit N`은 처리한 행이 N개가 되면 멈춘다. 건너뛴 행(이미 체크된 행)은 세지 않는다.
- `--skip-phone-lookup`을 주면 전화번호 보완 조회를 하지 않는다(REQ-PHONE-001).
- `--sheet-csv PATH`를 주면 시트 대신 CSV 파일을 읽는다.
- `--defer-sheet-checks`를 주면 Sheets 자격 증명 없이도 `--execute`를 할 수 있다. 시트 체크는
  따로 하는 일로 미룬다. 평소 실행에서는 쓰지 않는다.

#### 기대 결과
- 실행을 시작하면 실행 방식과 처리할 행 수가 로그에 남는다.
- 옵션을 잘못 섞어 줘도 드라이런이 실제 입력으로 바뀌지 않는다.

#### 예외 처리
- Playwright가 설치되어 있지 않으면 설치 명령을 알려 주고 끝낸다.
- `--execute`를 줬고 `--defer-sheet-checks`는 주지 않았는데 OAuth 클라이언트 파일이 없으면,
  입력을 시작하지 않고 멈춘다.

#### 관련 구현
`main.py`, `completion_automation.py`: `parse_args`, `Options`, `main`

#### 관련 테스트
TEST-DRYRUN-001, TEST-EXEC-001

### REQ-FILTER-001 처리할 행 고르기

#### 목적
이미 처리한 행과 입력하면 안 되는 교육과정은 처리 대상에서 뺀다.

#### 동작
`입력여부`가 체크로 읽히는 값이면 그 행은 검색도 하지 않고 건너뛴다. 체크로 보는 값은 다음과
같고, 대문자와 소문자를 가리지 않는다.

- `TRUE`, `Y`, `YES`
- `완료`, `입력완료`
- 체크 기호

`구분` 값은 교육과정 매핑 표로 바꾼다. 기본으로 허용하는 과정은 다음 다섯 개다.

| 시트 `구분` | 디모데 과정 |
|---|---|
| LTC | 청년-LTC |
| 확신반 | 청년-확신반 |
| 성장반 | 청년-성장반 |
| 성경대학 | 청년-성경대학 |
| 교리대학 | 청년-교리대학 |

설정으로 매핑을 더할 수 있다. 하지만 제외 과정(`기초반`, `예배학교`, `결혼예비학교`)은 설정에
넣어도 코드가 다시 뺀다. 설정만으로 제외 과정을 입력 대상에 넣을 수 없어야 한다.

매핑 결과가 없거나 `청년-`으로 시작하지 않으면 `교육과정매핑없음`으로 남기고 입력하지 않는다.

#### 기대 결과
- 다시 실행해도 이미 체크된 행은 또 처리되지 않는다.
- 제외 과정은 어떤 길로도 디모데에 입력되지 않는다.

#### 예외 처리
허용 목록에 없는 `구분`은 오류로 보지 않는다. 처리하지 않은 정상 상태로 기록하고 실행을 계속한다.

#### 관련 구현
`completion_automation.py`: `is_checked`, `course_map`, `process`

#### 관련 테스트
TEST-DRYRUN-001

### REQ-MATCH-001 전화번호로 교인 찾기

#### 목적
전화번호가 있는 행은 전화번호를 1순위로 보고 동일인을 확정한다.

#### 동작
1. 시트의 `핸드폰`(또는 `전화번호`, `연락처`) 값에서 숫자만 남긴다.
2. 숫자가 10자리나 11자리면 `000-0000-0000` 모양으로 맞춘다. 다른 길이면 번호가 없는 것으로 본다.
3. 맞춘 번호로 디모데를 검색한다. 결과 카드 가운데 `이름`과 `전화번호`가 모두 같은 것을 고른다.
4. 그런 카드가 정확히 하나면 그 사람으로 확정한다. 군·팀이 달라도 같은 사람으로 본다.
   이유: 시트의 소속은 예전 정보일 수 있고, 이름과 번호를 함께 보는 쪽이 사람을 더 확실히 가려낸다.
5. 둘 이상이면 확정하지 않고 `교인복수일치`로 멈춘다.

#### 기대 결과
번호가 있는 행은 소속이 바뀌었어도 같은 사람을 찾아간다. 찾지 못하면 처리하지 않은 채로 안전하게
남는다.

#### 예외 처리
- 번호로 확정하지 못하면(0건) 실패로 끝내지 않는다. REQ-MATCH-002의 이름으로 찾는 길로 넘어간다.
- 여러 명이 맞으면 이름으로 찾는 길로 넘어가지 않고 그 자리에서 멈춘다.

#### 관련 구현
`completion_automation.py`: `format_phone`, `search_person_cards`, `parse_person_card`,
`find_exact_person_card`

#### 관련 테스트
TEST-DRYRUN-001

### REQ-MATCH-002 이름과 소속으로 교인 찾기

#### 목적
전화번호로 확정하지 못한 사람은 이름과 소속으로 확정한다. 조금이라도 애매하면 입력하지 않는다.

#### 동작
이름으로 검색한다. 결과 카드 가운데 다음을 **모두** 만족하는 것만 후보로 본다.

- 이름이 시트의 `이름`과 같다.
- `군`이 시트의 `군`과 같다. 비교하기 전에 괄호와 그 안의 세부 이름을 뗀다.
- 디모데 교인 구분이 `교인 > 청년 (A)` 또는 `(B)`이다.

> **예시** `신군(아하)`는 `신군`으로 바꾼 뒤 비교한다.

후보 수에 따라 이렇게 한다.

| 후보 | 처리 |
|---|---|
| 정확히 한 명 | 그 사람으로 확정한다. |
| 두 명 이상 | `교인복수일치`로 남긴다. |
| 0명 | `정확한교인없음`으로 남긴다. |

이 길로 확정한 사람에게 디모데 전화번호가 있으면, 그 번호를 시트에 적을 값으로 삼는다
(REQ-SHEET-002).

#### 기대 결과
동명이인이 있거나 청년 A/B가 아닌 사람은 절대 입력 대상이 되지 않는다.

#### 예외 처리
애매하면 사람이 판단하도록 처리하지 않은 채로 남긴다. 짐작으로 한 명을 고르지 않는다.

#### 관련 구현
`completion_automation.py`: `normalize_group`, `same_name_and_group`,
`same_active_name_and_group`, `find_exact_person_card`

#### 관련 테스트
TEST-DRYRUN-001

### REQ-PHONE-001 비어 있는 전화번호 채우기

#### 목적
시트에 전화번호가 비어 있으면 디모데에서 번호를 찾아 채운다. 그러면 사람을 더 정확히 찾을 수 있다.

#### 동작
1. 번호가 비어 있고 `--skip-phone-lookup`이 꺼져 있을 때만 한다.
2. 이름으로 검색해 후보를 모은다. 후보는 `이름+군`이 같고, 교인 구분이 청년 A/B이고, 전화번호가
   있는 사람이다.
3. 후보들의 번호를 모았을 때 서로 다른 번호가 정확히 하나뿐일 때만 그 번호를 쓴다. 번호가 여러
   개면 쓰지 않는다.
4. 쓰기로 한 번호는 이어서 REQ-MATCH-001의 검색에 쓴다. 성공하면 시트에도 적는다.

#### 기대 결과
- 번호가 없다는 이유만으로 확정에 실패하는 행이 줄어든다.
- 시트의 빈 번호 칸이 확인된 값으로 채워진다.

#### 예외 처리
번호를 찾지 못해도 오류가 아니다. 번호 없는 채로 REQ-MATCH-002의 길을 계속 간다.

#### 관련 구현
`completion_automation.py`: `enrich_phone`

#### 관련 테스트
TEST-DRYRUN-001

### REQ-INPUT-001 같은 수료내역 두 번 등록하지 않기

#### 목적
같은 사람에게 같은 교육과정이 두 번 등록되지 않게 한다.

#### 동작
- 저장하기 바로 전에 교인 상세 창에서 대상 교육과정 이름이 이미 목록에 있는지 본다.
- 있으면 저장하지 않고 상태를 `이미입력됨`으로 남긴다. 이 경우는 정상적으로
  끝난 것으로 본다.
- 이 확인은 실행할 때마다 디모데 화면에서 새로 한다. 시트의 체크 상태만 믿지 않는다.

#### 기대 결과
디모데에 저장한 뒤 시트에 체크하기 전에 프로그램이 멈춰도, 다시 실행했을 때 두 번 등록되지 않는다.

#### 예외 처리
상세 창을 열지 못하거나 확인할 수 없으면 저장하지 않는다. 확인하지 않고 저장하는 일은 허용하지
않는다.

#### 관련 구현
`completion_automation.py`: `process`

#### 관련 테스트
TEST-DUP-001

### REQ-INPUT-002 수료내역 등록

#### 목적
확정된 교인에게 정해진 값으로 수료내역을 등록한다.

#### 동작
다음 값을 넣는다.

| 칸 | 값 |
|---|---|
| 교육과정 | REQ-FILTER-001에서 바꾼 `청년-` 과정명. 이름이 정확히 같은 것을 고른다. |
| 상태 | `수료` |
| 수료일 | `20260621` |
| 수료연도 | `2026` |
| 강사명 | 시트의 `담당자` 값 |

- 저장한 뒤 설정된 시간만큼 기다리고 상태를 `입력완료`로 남긴다.
- 저장했든 못 했든 교인 상세 창은 닫는다.

#### 기대 결과
사람이 디모데 화면에서 직접 입력했을 때와 같은 값이 남는다.

#### 예외 처리
- 수료일과 수료연도는 지금 코드에 고정된 값(상수)이다. 기수가 바뀌면 사양과 코드를 함께 고쳐야
  한다(13절 참조).
- 화면 요소를 찾지 못하면 그 행은 실패로 끝난다. 근거 없이 선택자를 짐작해서 고치지 않는다.

#### 관련 구현
`completion_automation.py`: `process`, 상수 `COMPLETE_STATUS`, `COMPLETE_DATE`,
`COMPLETE_YEAR`

#### 관련 테스트
TEST-EXEC-001

### REQ-SHEET-002 입력여부 체크 맞추기

#### 목적
시트의 `입력여부` 체크가 "디모데에 정말 등록되어 있다"는 뜻이 되도록 유지한다.

#### 동작
- 체크는 다음 두 경우에만 한다.
  - 새로 저장에 성공했을 때
  - 같은 과정이 이미 등록되어 있는 것을 확인했을 때
- 체크할 행은 REQ-SHEET-001이 붙인 원본 시트 행 번호로 찾는다.
- 새로 찾은 전화번호가 있으면 같은 때에 `핸드폰` 열에도 적는다.
- 확정 실패, 여러 명 일치, 과정 매핑 없음인 행은 체크하지 않는다.

#### 기대 결과
체크되지 않은 행은 언제나 다시 실행할 대상이 된다. 사람이 시트 값을 고친 뒤 다시 돌리면 된다.

#### 예외 처리
Sheets 자격 증명이 없으면 체크하지 않는다. 이 상태로 실제 입력을 하려면 `--defer-sheet-checks`를
직접 줘야 한다. 이때 체크는 운영자가 따로 맞춘다.

#### 관련 구현
`completion_automation.py`: `update_sheet_result`, `get_sheets_service`

#### 관련 테스트
TEST-EXEC-001

### REQ-REPORT-001 입력하지 않은 이유 알리기

#### 목적
무엇을 왜 입력하지 않았는지 실행이 끝난 뒤 사람이 확인할 수 있게 한다.

#### 동작
처리한 행마다 다음 상태 가운데 하나를 남긴다.

| 상태 | 의미 |
|---|---|
| `드라이런확인` | 교인을 확정했지만 저장하지 않았다 |
| `입력완료` | 새 수료내역을 저장했다 |
| `이미입력됨` | 같은 과정이 이미 있어서 저장하지 않았다 |
| `정확한교인없음` | 전화번호로도, 이름·군·A/B로도 확정하지 못했다 |
| `교인복수일치` | 조건에 맞는 사람이 여러 명이라 멈췄다 |
| `교육과정매핑없음` | 허용되지 않았거나 제외된 과정이다 |

결과는 CSV 파일로 `output/` 아래에 저장한다. 열은
`시트행 / 구분 / 군 / 팀 / 이름 / 핸드폰 / 담당자 / 교육과정 / 상태`이다.

| 실행 방식 | 파일 이름 |
|---|---|
| 드라이런 | `dry_run_plan.csv` |
| 실제 입력 | `execution_result.csv` |

행을 하나 처리할 때마다 `result_log.txt`에 한 줄씩 덧붙인다. 그래서 실행이 중간에 끊겨도 어디까지
했는지 남는다.

#### 기대 결과
실행 뒤 CSV만 보고 다시 작업할 행을 고를 수 있다.

#### 예외 처리
결과 CSV는 실행할 때마다 같은 파일 이름으로 덮어쓴다. 보관해야 하면 실행 뒤 사람이 복사해 둔다.

#### 관련 구현
`completion_automation.py`: `save_plan`, `log`, `process`

#### 관련 테스트
TEST-DRYRUN-001

## 6. 비기능 요구사항

### NFR-SEC-001 인증 정보는 저장소 밖에 둔다

#### 요구
로그인에 쓰는 정보는 저장소 바깥에 둔다. 어떤 결과물에도 그 값이 드러나면 안 된다.

- 디모데 계정, Google 쿠키, Sheets OAuth 클라이언트·토큰 JSON은 `config.py`와 로컬 JSON 파일에만
  둔다. 이 파일들은 `.gitignore`에 올라 있어야 한다.
- `config.example.py`에는 자리만 잡아 두는 값만 넣는다. 실제 값이 들어가면 안 된다.
- 로그, 결과 CSV, 커밋 메시지, 에이전트 응답에 자격 증명을 옮겨 적지 않는다.
- OAuth 접근 범위(scope)는 Sheets 접근 하나로 제한한다.

#### 측정
- 저장소가 관리하는 파일에 자격 증명이 0건이다.
- `.gitignore`가 `config.py`, 인증 JSON, `output/`, `user_data/`, `result_log.txt`, 디버그 결과물을
  모두 담는다.

### NFR-DATA-001 개인정보는 이 컴퓨터 안에서만 다룬다

#### 요구
이 자동화가 다루는 값은 실명·연락처·소속이 들어간 개인정보다. 그래서 이 컴퓨터(로컬) 안에서만
다룬다.

- 결과 CSV, 실행 로그, 브라우저 프로필, 디버그 HTML·이미지는 로컬에만 남긴다. 저장소에 올리지 않는다.
- 개인정보를 Knowledge, Evidence, 사양 문서, 이슈에 옮겨 적지 않는다. 필요하면 건수와 사유 종류만 쓴다.
- 시트에는 `핸드폰`(E열)과 `입력여부`(G열) 두 칸만 쓴다. 다른 열은 덮어쓰지 않는다.

#### 측정
- 저장소에 커밋된 개인정보 파일이 0건이다.
- 시트에 쓰는 열은 2개로 고정되어 있다.

### NFR-OPS-001 사람이 지켜보며 직접 실행한다

#### 요구
사람이 지켜보는 상태에서 직접 실행한다. 대상 사이트에 부담을 주지 않는다.

- Chromium은 화면이 보이게 띄운다. 사람 없이 돌리거나 화면 없이(headless) 돌리는 것을 전제로 하지
  않는다.
- 검색한 뒤 기본 0.8초, 저장한 뒤 기본 1.0초를 기다린다. 설정으로 늘릴 수 있다.
- 실행 순서는 항상 다음과 같다.
  1. `--limit`을 건 드라이런
  2. 전체 드라이런
  3. `--execute --limit 1`
  4. 전체 실행
- 예약 실행 도구(스케줄러)나 CI에서 실행하지 않는다.

#### 측정
- 한 번 실행하는 동안 검색 요청 사이 간격은 최소 0.8초다.
- 사람 없이 도는 실행 경로는 0개다.

## 7. 데이터 사양

| 데이터 | 형태 | 위치 | 취급 |
|---|---|---|---|
| 수료자 명단 | 7열 표 (`구분 / 군 / 팀 / 이름 / 핸드폰 / 담당자 / 입력여부`) | Google Sheet 대상 탭 | 원본이다. 이 자동화는 E·G열만 쓴다. |
| 디모데 교인 정보 | 검색 결과 카드 글자에서 뽑은 이름·전화번호·군·팀·교인구분 | 메모리에만 있다 | 저장하지 않는다. |
| 처리 결과 | 9열 CSV | `output/` | 실행할 때마다 덮어쓴다. 저장소에 넣지 않는다. |
| 실행 로그 | 행마다 한 줄씩 덧붙이는 텍스트 | `result_log.txt` (설정으로 바꿀 수 있음) | 계속 쌓인다. 저장소에 넣지 않는다. |
| 브라우저 프로필·디버그 결과물 | Playwright 데이터, HTML·PNG | 프로젝트 안 로컬 폴더 | 원인을 찾을 때만 열어 본다. 저장소에 넣지 않는다. |
| 자격 증명 | 설정 파일(Python 모듈), OAuth 클라이언트·토큰 JSON | 프로젝트 로컬 | NFR-SEC-001을 따른다. |

보관 기간은 정해져 있지 않다(13절 참조). 결과 CSV와 로그는 사람이 직접 정리한다.

## 8. Configuration 사양

설정은 `config.py` 한 파일에서 읽는다. 예시는 `config.example.py`에 있다.

| 항목 | 필수 | 기본값 | 의미 |
|---|---|---|---|
| `SHEET_URL` | 예 | 없음 | 대상 스프레드시트 주소. 여기서 스프레드시트 ID와 탭을 가리키는 gid를 뽑는다. |
| `SHEET_TAB_NAME` | 아니오 | `통합 명단` | 읽고 쓸 탭 이름. |
| `GOOGLE_COOKIES` | 아니오 | 없음 | OAuth가 없을 때 CSV로 내려받는 데 쓰는 쿠키. 만료되면 401 오류가 난다. |
| `DIMODE_URL` | 예 | 없음 | 디모데 로그인 페이지 주소. |
| `USER_ID` / `USER_PW` | 예 | 없음 | 디모데 계정. |
| `GOOGLE_OAUTH_CLIENT_FILE` | 실제 입력 시 예 | 프로젝트 안 클라이언트 JSON | 데스크톱용(Desktop) OAuth 클라이언트 파일 경로. |
| `GOOGLE_OAUTH_TOKEN_FILE` | 아니오 | 프로젝트 안 토큰 JSON | 승인한 뒤 저절로 만들어지고 갱신되는 토큰 파일 경로. |
| `EDU_COURSE_MAP` | 아니오 | 비어 있음(빈 dict) | 기본 매핑에 더할 과정. 제외 과정은 넣어도 무시된다. |
| `LOG_FILE` | 아니오 | `result_log.txt` | 실행 로그 경로. 상대 경로는 프로젝트 폴더 기준이다. |
| `SEARCH_DELAY` | 아니오 | 0.8 | 검색한 뒤 기다리는 초. |
| `SAVE_DELAY` | 아니오 | 1.0 | 저장한 뒤 기다리는 초. |

나머지는 실행 옵션으로 받는다(REQ-CLI-001). 환경변수는 쓰지 않는다.

## 9. 오류 처리 정책

### 다시 시도하지 않는다

검색이나 저장이 실패해도 저절로 다시 해 보지 않는다. 다시 할 때는 사람이 원인을 확인한 뒤
프로그램을 다시 실행한다.
이유: 중복 방지(REQ-INPUT-001)가 있어서 다시 실행해도 안전하다.

### 일부만 성공해도 계속한다

확정 실패, 여러 명 일치, 과정 매핑 없음은 그 행의 상태로 기록하고 다음 행으로 넘어간다. 한 사람
때문에 전체 실행을 멈추지 않는다.

### 실행을 멈추는 경우

다음은 행의 상태로 남기지 않고 실행 전체를 멈추는 사유다.

- `SHEET_URL`에서 스프레드시트 ID를 얻지 못했다.
- 쿠키 만료(401)로 명단을 읽지 못했다.
- `--execute`인데 필요한 OAuth 클라이언트 파일이 없다.
- Playwright가 설치되어 있지 않다.
- 로그인에 실패했거나 화면 구조가 바뀌어 검색 화면까지 가지 못했다.

### 기록 위치

- 행마다의 결과는 `output/`의 CSV와 `result_log.txt`에 남는다.
- 로그는 행을 처리할 때마다 바로 덧붙인다. 그래서 중간에 멈춰도 어디까지 했는지 알 수 있다.

> **주의** 결과 CSV는 실행이 끝까지 가야 저장된다.

### 선택자가 맞지 않을 때

디모데 화면 구조가 바뀐 것 같으면 실제 입력을 멈추고 드라이런부터 다시 확인한다. 근거 없이 선택자를
바꾸지 않는다.

## 10. 보안 요구사항

- 로그인 수단은 두 가지다.
  - 디모데 ID와 비밀번호(설정 파일)
  - Google Sheets OAuth 토큰(로컬 JSON)
- 쿠키 방식은 드라이런에서 임시로만 쓸 수 있다. 운영에는 쓰지 않는다.
- 저장소에 들어가면 안 되는 것: `config.py`, OAuth 클라이언트·토큰 JSON, 쿠키 값, `output/`,
  `user_data/`, `result_log.txt`, 디버그 HTML·이미지.
- 접근 범위: Google 쪽은 Sheets 접근 권한(scope) 하나만 쓴다. 디모데 쪽은 그 계정이 원래 가진
  청년관리 권한을 넘지 않는다. 이 자동화는 권한을 넓히지 않는다.
- 토큰이 만료되면 갱신용 토큰(refresh token)으로 새로 받는다. 받은 결과는 같은 로컬 파일에 다시 적는다.
- 에이전트는 설정 파일과 자격 증명 파일의 내용을 열어 응답에 싣지 않는다.

## 11. 테스트 사양

ID 규칙: `TEST-<CATEGORY>-NNN`.

이 프로젝트에는 **자동으로 도는 테스트가 없다**. 테스트 도구(프레임워크)도, 테스트 파일도 없다.

아래 항목은 모두 사람이 직접 하는 확인 절차다. 추적성 표의 Test 열에는 `(수동)`으로 표시한다.
자동 테스트를 들일지는 13절에 적은 아직 정하지 않은 일이다.

### TEST-SYNTAX-001

#### 검증 대상
REQ-CLI-001, REQ-REPORT-001 (코드 문법이 깨지지 않았는지 확인한다)

#### 선행 조건
프로젝트 가상환경이 설치되어 있다. 바깥 시스템에 접속할 필요는 없다.

#### 절차
1. 프로젝트 루트에서 `.venv/bin/python -m py_compile main.py completion_automation.py config.example.py`를 실행한다.

#### Expected Result
아무것도 출력하지 않고 종료 코드 0으로 끝난다. 이 검사는 문법만 본다. 동작은 확인하지 않는다.

### TEST-DRYRUN-001

#### 검증 대상
REQ-SHEET-001, REQ-CLI-001, REQ-FILTER-001, REQ-MATCH-001, REQ-MATCH-002,
REQ-PHONE-001, REQ-REPORT-001

#### 선행 조건
설정이 끝나 있고, 디모데 로그인과 시트 접근이 된다. 바깥 시스템을 조회하게 되므로 사용자가 직접
요청했을 때만 한다.

#### 절차
1. `python main.py --limit 5`를 실행한다.
2. 브라우저가 뜨고 로그인과 검색이 진행되는 것을 화면으로 본다.
3. `output/` 아래 드라이런 계획 CSV를 연다.
4. 각 행의 `상태`가 `드라이런확인` / `정확한교인없음` / `교인복수일치` /
   `교육과정매핑없음` 가운데 하나인지 본다.
5. 번호가 비어 있던 행에 찾은 번호가 채워졌는지 본다.
6. 대상 시트를 열어 아무 값도 바뀌지 않았는지 확인한다.

#### Expected Result
- 디모데와 시트가 전혀 바뀌지 않는다.
- 처리한 행 수가 5를 넘지 않는다.
- 각 행에 판정 사유가 남는다.

### TEST-EXEC-001

#### 검증 대상
REQ-INPUT-002, REQ-SHEET-002, REQ-CLI-001

#### 선행 조건
- TEST-DRYRUN-001을 마쳤고 결과에 이상이 없다.
- OAuth 승인을 마쳤다.
- 사용자가 실제 입력을 직접 요청했다.

#### 절차
1. `python main.py --execute --limit 1`을 실행한다.
2. 디모데에서 그 교인의 상세 화면을 직접 열어 교육과정·상태·수료일·수료연도·강사명을 눈으로
   확인한다.
3. 시트에서 그 행의 `입력여부`가 체크되었는지 본다. 번호를 채웠다면 `핸드폰`이 채워졌는지도 본다.
4. 결과 CSV에서 그 행의 상태가 `입력완료`인지 본다.

#### Expected Result
- 한 명만 입력된다.
- 디모데에 들어간 값이 사양에 적힌 고정값과 같다.
- 성공한 그 행만 체크된다.

### TEST-DUP-001

#### 검증 대상
REQ-INPUT-001

#### 선행 조건
TEST-EXEC-001로 입력된 사람이 있다. 그 행의 `입력여부` 체크를 손으로 풀어 둔다.

#### 절차
1. 그 행이 대상이 되도록 `python main.py --execute --limit 1`을 다시 실행한다.
2. 디모데 상세 화면에서 같은 교육과정이 하나뿐인지 확인한다.
3. 결과 CSV의 상태를 본다.

#### Expected Result
상태가 `이미입력됨`이고 교육과정이 두 번 등록되지 않는다. 시트는 다시 체크된다.

### TEST-HYGIENE-001

#### 검증 대상
NFR-SEC-001, NFR-DATA-001

#### 선행 조건
없음. 자격 증명도 인터넷 연결도 쓰지 않는다. git이 관리하는 파일 목록과 소스만 읽으므로 언제든
돌릴 수 있다.

> **참고** 이 프로젝트의 다른 TEST는 모두 사람이 하는 절차다. 이 둘(NFR-SEC-001, NFR-DATA-001)의
> '측정'은 프로그램이 답할 수 있다.

#### 절차
`.venv/bin/python -m unittest discover -s tests`를 실행한다
(`tests/test_repository_hygiene.py`).

#### Expected Result
- `.gitignore`가 `config.py`, `credentials.json`, `token.json`, `output/`, `user_data/`,
  `result_log.txt`, `debug_*.html`, `debug_*.png`를 모두 담는다.
- git이 그 경로들을 정말 무시하는지도 본다. 적어 둔 규칙과 git의 동작을 따로 확인한다.
  이유: `.gitignore` 뒤쪽에 있는 '무시하지 않기' 규칙(negation)이 앞의 규칙을 덮을 수 있다.
- git이 관리하는 파일에 민감한 결과물이 0건이다.
- `config.example.py`에 실제 스프레드시트 ID나 이메일 주소가 없다.
- `update_sheet_result`가 만드는 시트 범위(range)의 열이 정확히 `E`와 `G`다.
- 시트에 쓰는 함수가 그 하나뿐이다.
  이유: 다른 쓰기 길이 생기면 열 검사가 그 범위를 놓친다.

## 12. 요구사항 추적성

| Requirement | Implementation | Test | Status |
|---|---|---|---|
| REQ-SHEET-001 | `completion_automation.py` (`load_sheet_rows`) | TEST-DRYRUN-001 (수동) | verified |
| REQ-CLI-001 | `main.py`, `completion_automation.py` (`parse_args`, `main`) | TEST-DRYRUN-001 (수동) | verified |
| REQ-FILTER-001 | `completion_automation.py` (`is_checked`, `course_map`, `process`) | TEST-DRYRUN-001 (수동) | verified |
| REQ-MATCH-001 | `completion_automation.py` (`find_exact_person_card`) | TEST-DRYRUN-001 (수동) | verified |
| REQ-MATCH-002 | `completion_automation.py` (`same_active_name_and_group`) | TEST-DRYRUN-001 (수동) | verified |
| REQ-PHONE-001 | `completion_automation.py` (`enrich_phone`) | TEST-DRYRUN-001 (수동) | verified |
| REQ-INPUT-001 | `completion_automation.py` (`process`) | TEST-DUP-001 (수동) | implemented |
| REQ-INPUT-002 | `completion_automation.py` (`process`) | TEST-EXEC-001 (수동) | verified |
| REQ-SHEET-002 | `completion_automation.py` (`update_sheet_result`) | TEST-EXEC-001 (수동) | verified |
| REQ-REPORT-001 | `completion_automation.py` (`save_plan`, `log`) | TEST-DRYRUN-001 (수동) | verified |
| NFR-SEC-001 | `.gitignore`, `config.example.py` | TEST-HYGIENE-001 | verified |
| NFR-DATA-001 | `.gitignore`, `completion_automation.py` (`update_sheet_result`) | TEST-HYGIENE-001 | verified |
| NFR-OPS-001 | `completion_automation.py` (`main`, `search_person_cards`) | TEST-DRYRUN-001 (수동) | implemented |

Status 값: `draft` (사양만 있음) / `implemented` / `verified` (실제 실행까지 확인) /
`deprecated`.

`verified`의 근거는 `README.md`에 적힌 2026-08-11 실제 실행이다. 그 실행의 결과는 다음과 같다.

- 대상 206행
- 체크 174행
- 요청에 따라 뺀 행 31행
- 확정 실패 1행

REQ-INPUT-001은 `implemented`로 둔다.
이유: 그 실행에서 `이미입력됨` 경로가 나온 적이 있는지 기록이 남아 있지 않다.

## 13. 미확정 사항

- **수료일·수료연도가 코드에 고정되어 있다.** `20260621` / `2026`이 고정값이라 다음 기수에는 반드시
  코드를 고쳐야 한다. 이 값을 설정 항목이나 실행 옵션으로 뺄지, 뺀다면 시트의 열로 받을지 정하지 않았다.
- **제외 교육과정을 결국 어떻게 할지.** `기초반`, `예배학교`, `결혼예비학교`는 "추후 확정 후
  사용자 요청 시"라고만 적혀 있다. 디모데의 정확한 과정 이름도, 입력할지도 정하지 않았다.
- **불리지 않는 팀 비교 함수.** 확정하는 길은 `이름 + 군 + 청년 A/B`이고, `팀`은 판정에 쓰지
  않는다(2026-09-19 확정, 문서를 코드에 맞춤).
  - `identity_matches`, `normalize_team`은 만들어만 두었고 어디서도 부르지 않는다.
  - 이 둘을 지울지, 시트 소속 확인에 쓸지는 정하지 않았다.
- **`--defer-sheet-checks`를 쓴 뒤 할 일.** 미룬 체크를 누가 어떻게 맞출지 정한 절차가 없다.
  - 이 옵션을 켜도 OAuth 파일이 있으면 체크가 그대로 된다.
  - 이것이 의도인지(자격 증명이 있으면 체크한다), 아니면 늘 미뤄야 하는지 정하지 않았다.
- **자동 테스트를 어디까지 들일지.** 지금은 테스트가 없다. 입력만으로 결과가 정해지는 함수
  (`format_phone`, `normalize_group`, `is_checked`, `course_map`)만 단위 테스트로 확인할지,
  Playwright 경로도 대상으로 할지 정하지 않았다.
- **결과 CSV와 실행 로그를 얼마나 둘지.** 개인정보가 들어 있는데 정리 주기나 지울 기준이 없다.
- **Owner.** 저장소에 운영 책임자가 적혀 있지 않다.

## 14. 향후 개선 후보

- 수료일·수료연도·상태를 시트 열이나 실행 옵션으로 받는다. 그러면 기수마다 코드를 고치지 않아도 된다.
- 결과 CSV 파일 이름에 실행 시각을 넣는다. 그러면 이전 실행 결과를 덮어쓰지 않는다.
- 입력만으로 결과가 정해지는 함수에 단위 테스트를 더한다. 저장해 둔 HTML 화면 파일(fixture)로
  `parse_person_card`를 확인하는 테스트도 더한다.
- 디모데 화면 선택자를 한곳에 모은다. 그러면 화면이 바뀔 때 고칠 곳이 줄어든다.
- `정확한교인없음` 행만 모아 후보 목록과 함께 출력한다. 그러면 사람이 판단하기 쉬워진다.
